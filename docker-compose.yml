version: '3.9'

services:
  web:
    build: ./web/
    image: moe_web
    volumes:
      - ./web/:/app/
  api:
    build:
      context: .
      dockerfile: ./Dockerfile
    image: moe_api
    volumes:
      - ./moe/:/moe/moe
      - ./web/dist/:/app/dist/
      # - ./web/static/:/app/static/
    environment:
      - WEB_DIST=/app/dist/
    ports:
      - 5000:80
  # ingest:
  #   build: ./ingest/
  #   image: moe_ingest
  #     volumes:
  #       - ./ingest/:/app/
  # mongo:
  #   image : mongo
  #   container_name: moe_mongo
  #   environment:
  #     - PUID=1000
  #     - PGID=1000
  #   volumes:
  #     - ./db/:/data/db
  #   ports:
  #     - 27017:27017
  #   restart: unless-stopped
